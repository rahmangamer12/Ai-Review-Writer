# Specs Index

Registry of all AutoReview AI feature specs. One row per spec folder. Keep newest at top.

How to use: run `/speckit-specify <feature>` to create `specs/NNN-feature/spec.md` and add a row here.
Then `/speckit-plan` → `/speckit-tasks` → `/speckit-implement`. See `specs/README.md` for the full workflow.

| ID  | Feature | Status | Folder | Notes |
| --- | ------- | ------ | ------ | ----- |
| —   | _No numbered feature specs yet_ | — | — | Use `/speckit-specify` to add the first one |

## Reference docs (not numbered specs)

- `specs/README.md` — spec-driven workflow, template, Definition of Ready/Done
- `specs/context-engineering.md` — context-efficiency guidance

## Status legend

Draft → Approved → In Progress → Shipped
